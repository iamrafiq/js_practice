_222‍.x([["default",()=>_222‍.o]]);class Finder{
    constructor(name, courses = []){
        this.name = name;
        this.courses = courses;
    }

    find(itemName){
        const index = this.courses.map((currentItem)=>currentItem.name).indexOf(itemName);
        return this.courses[index];        

    }
}

_222‍.d(Finder);